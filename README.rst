pretalx Sphinx Theme
====================

Sphinx theme used by pretalx_.

Installation
============

Install via pip::

    $ pip install pretalx_sphinx_theme

or if you have the code checked out locally::

    $ python setup.py install

.. _pretalx: https://docs.pretalx.org
